# report-renderers

A small library that renders report rows through named output adapters. JSON is
currently registered; applications may register another renderer through the
same public extension boundary.
