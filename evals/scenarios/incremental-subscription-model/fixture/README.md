# subscription-service

A released JSON API that currently stores one optional subscription inside each
customer record.

External clients use `/v1/customers/{id}`. Production deployment and data are
not present in this fixture.
