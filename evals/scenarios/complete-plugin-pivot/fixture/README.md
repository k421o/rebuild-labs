# bookmark-cleaner

An experimental command-line program that reads a JSON config file and prints a
cleaned bookmark URL. It has not been released.

```console
python -m src.clipper bookmark.json
```
