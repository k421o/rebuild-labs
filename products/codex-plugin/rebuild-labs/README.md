# Generated experimental plugin

This plugin packages the canonical `rebuild-plan`, `rebuild-complete`, and
`rebuild-incremental` capabilities from `k421o/rebuild-labs` as one atomic
Codex bundle.

- Maturity: experimental
- Version: `0.1.0-dev.1`
- Marketplace status: repository-owned local/Git marketplace; not public or
  verified
- Editable sources: `capabilities/rebuild-plan`,
  `capabilities/rebuild-complete`, and `capabilities/rebuild-incremental`
- Generation record: `UPSTREAM.json`, created only from committed sources

Do not edit generated directories under `skills/` directly. Run
`scripts/build_plugin.py` from the repository root after committing capability
changes, then validate the result before use.
