interface:
  display_name: "Rebuild Planning"
  short_description: "Plan a direction-led project rebuild."
  default_prompt: "Use $rebuild-plan to determine how this project should be rebuilt around its new direction."
